# Augment

Add IgDiscover-specific columns to the AIRR assignment table: alignment error counts, coverage (V/D/J), CDR3 coordinates, and header-derived fields (size, barcode).

```@autodocs
Modules = [IgDiscover]
Pages = ["augment.jl"]
```
