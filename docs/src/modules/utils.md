# Utilities

Internal helpers used across the pipeline: tallying, gene name comparison, DataFrame column helpers, and TOML parsing for config structs.

```@autodocs
Modules = [IgDiscover]
Pages = ["utils.jl"]
```
